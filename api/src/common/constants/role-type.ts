'use strict';

export enum RoleType {
  USER = 'USER', //otros usuarios tipo secretaria u otos
  ADMIN = 'ADMIN', //admin del sistena
  CLIENT = 'CLIENT', // cliente ue hace pedidos de carros
  DRIVER = 'DRIVER', //conductor
}
